{
  "name": "nosaic-success-api",
  "description": "Nosaic Success API",
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces",
    "build": "npm run build --workspaces",
    "deployCloud": "npm run deploy --workspace=@nosaic-success/cloudflare"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
